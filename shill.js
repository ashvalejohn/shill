// STEP 2
// Grab all links in the #description box
console.log("RUNNING shill.js");
const links = setTimeout(() => { console.log(document.querySelectorAll('#description > a'));}, 5000);


// // STEP 3
// Send URLs to background as message

// STEP 5
// Display product cards
